[project]
name = "testomat-tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "faker==37.12.0",
    "playwright==1.55.0",
    "pytest>=8.0.0,<9.0.0",
    "pytest-html==4.1.1",
    "pytest-playwright==0.7.1",
    "python-dotenv==1.2.1",
    "ruff>=0.15.0",
    "requests==2.32.5"
]

[dependency-groups]
dev = [
    "ruff>=0.15.0",
]
